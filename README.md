# Kawaii Artist Site

ピンクと薄い水色を基調にした、同人アーティスト向けのNext.jsサイトテンプレートです。

## ローカル起動

```bash
npm install
npm run dev
```

ブラウザで http://localhost:3000 を開きます。

## 編集する場所

主に `app/page.tsx` の以下を変更します。

- `profile`
- `snsLinks`
- `discography`
- `goods`

色やアニメーションは `app/globals.css` で調整できます。
